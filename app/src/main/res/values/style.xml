<resources>

    <!-- Project level resources -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>


    <style name="loanrequest" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- LoginActivity -->
    <style name="LoginActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- SignupActivity -->
    <style name="SignupActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- OnboardActivity -->
    <style name="OnboardActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- ProfileActivity -->
    <style name="ProfileActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- SplashActivity -->
    <style name="SplashActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- RequestLoanActivity -->
    <style name="RequestLoadActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- HomeActivityActivity -->
    <style name="HomeActivityActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- BorrowerRejectPageActivity -->
    <style name="BorrowerRejectPageActivityTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- LenderReviewPageActivity -->
    <style name="LenderReviewPageActivityTheme" parent="Theme.AppCompat.NoActionBar" />
</resources>